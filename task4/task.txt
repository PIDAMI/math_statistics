Гипотезы:

1) простые гипотезы - N(mu, sigma2), B(p,n), P(lambda), Exp(lambda)

x1 ~ N(5,7) n=200, x2 ~ N(5,9) n=300 - строим 2 независимые выборки; 
t = (x1.mean - x2.mean) / (s sqrt(1/n1 + 1/n2)) ~ t_{n1+n2-2} 
s2 = ((n1-1)S_n1^2 + (n2-2)S_n2^2) / (n1+n2-2)     

строим для группированных и негруппированных(меняется при группировании кол-во
степеней свободы)

2)n=200, x ~ N(3, 12), y ~ 5x + 0.1 Uniform(-6,6);
t = z.mean/(s_n(z)/ sqrt(n)) ~ t_{n-1} 
z = x-y

3) x ~ P(9), n=200; делим пополам выборку и проверяем гипотезу,
что в левой и правой половине одинаковый параметр 

4) проверяем гипотезу о равенстве дисперсий по критерию фишера для даты из 1

5) разбить данные из 1(х2) и разбить на два куска и проверить равенство 
дисперсий в правой и левой части с помощью фишера

6) считаем коэф корреляции для данных из 2 ro, делаем проебразование
ro -> ro / sqrt(1-r^2) * sqrt(n-2) ~ t_{n-2}
гипотезу ставим о некоррелированности

7) х ~ N(3,12) делим пополам и проверяем, что эти два куска некоррелированы
сравнить с результатами при преобразовании фишера

Критерии согласия

1) x1 ~ N(5,7) n=400 k = 10; 

x2 ~ x1 + 0.1 U(-6,6), x3 ~ x1 + 0.01 Cauchy(0,1)
для всего проверяем хи квадратом нормальность

группируем

F(x,theta^) = N(x.mean, x.var);

2) те же данные критерием колмогорова(должен быть менее мощным)

3) те же данные разбиваем на 2 и проверяем, что распределены половины 
одинаково распределены критерием колмогорова смирнова 
