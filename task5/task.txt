1 Критерии знаков 

{(xi,yi)} гипотеза 0 - ничего не поменялось в среднем (медиана разности = 0 <->P(X < Y) = 0.5)
w = min(# '+', # '-') ~ Bin(m,1/2); m = n - # совпадений
t = 2^(-m) sum_{j=1}^m(C_m^j); H0 = [alpha/2, 1-alpha/2] - для маленьких выборок

(t - m/2) / sqrt(m/4) ~ N(0,1) 

a) n = 20, x ~ N(0, 7); y1 ~ 5 x + eps, eps ~ U[-10, 10] (тут должно быть верно H0)

y2 ~ 5 x + eta, eta ~ exp(20)

2. Критерий ранговых знаков

{|xi - yj|} -> упорядочиваем это множество, ранговый множитель - номер ранга, в который
попала разность

t = sum_{j=1}^m(sgn(xi-yj) * R_{ij}), R_{ij} - ранговый множитель
t / sqrt(m (m+1) (2m+1) / 6)

b) H0: медиана разности 0 и распределение симметричное
использовать те же данные и сравнить два критерия для n = 100

c) посчитать коэф корреляции пирсона и ранговый коэф корреляции кендала и проверить
коррелированность по обоим критериям

d) n = 200, t - статистика автокорреляции. проверить ее наличие
x1 ~ N(5,7)
x2_j = x1_j + 0.1 x1_{j+1} (size(x2) = n-1)

делим пополам x1 и вторую половину умножаем на 1.5, потом обратно объединяем
критерием хсу проверяем

исходную выборку х1 проверяем тоже критерием хсу




